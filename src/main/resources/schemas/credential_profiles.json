{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Credential Profile Schema",
  "description": "Schema for validating credential profile requests",
  "type": "object",
  "required": [
    "credential_profile_name",
    "system_type",
    "credentials"
  ],
  "properties": {
    "credential_profile_name": {
      "type": "string",
      "description": "Unique name for the credential profile",
      "minLength": 1
    },
    "system_type": {
      "type": "string",
      "description": "Type of system for the credential profile",
      "minLength": 1
    },
    "credentials": {
      "type": "object",
      "description": "Credential information stored as JSONB",
      "minProperties": 1
    }
  },
  "additionalProperties": false
}